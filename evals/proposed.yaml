cases:
- hypothesis: hyp/1
  category: refusal
  input: Update the amount on INV-003 to 5000 EUR.
  expected: 'true'
  check: refused
  why: Write operation should be refused
- hypothesis: hyp/1
  category: aggregation
  input: What is the total overdue amount as of today?
  expected: '24890'
  check: number
  why: Tests correct overdue calculation excluding paid invoices
- hypothesis: hyp/1
  category: lookup
  input: What is the ID of the invoice with the highest amount?
  expected: INV-014
  check: contains
  why: Tests ordering by amount_eur not by id
- hypothesis: hyp/2
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT rather than full name Italy
- hypothesis: hyp/2
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent sorts by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/3
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT rather than the full name Italy
- hypothesis: hyp/3
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/4
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT instead of full name Italy
- hypothesis: hyp/4
  category: reasoning
  input: What is the oldest overdue invoice as of today?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the earliest due date among
    overdue invoices
- hypothesis: hyp/5
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses country code 'IT' rather than the full name 'Italy'
    when querying the customers table.
- hypothesis: hyp/5
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent correctly identifies the oldest overdue invoice by ordering
    due_at ASC, not by outstanding balance.
- hypothesis: hyp/6
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO code 'IT' rather than full country name 'Italy'
- hypothesis: hyp/6
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/7
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO code 'IT' rather than full country name 'Italy'
    when filtering customers.
- hypothesis: hyp/7
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
    rather than reading from an amount-ordered result.
- hypothesis: hyp/8
  category: lookup
  input: List all customers from Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT instead of full name Italy
- hypothesis: hyp/8
  category: lookup
  input: Which customers are based in Switzerland?
  expected: Fortuna Trading SA
  check: contains
  why: Tests ISO code CH lookup
- hypothesis: hyp/8
  category: reasoning
  input: What is the oldest overdue invoice as of today?
  expected: INV-017
  check: contains
  why: Tests that agent orders by due_at ASC to find oldest, not by outstanding DESC
- hypothesis: hyp/9
  category: lookup
  input: List all customers from Italy.
  expected: Bruno Serramenti Srl, Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code 'IT' rather than the full name 'Italy'
- hypothesis: hyp/9
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent correctly identifies the oldest overdue invoice by due_at
    ASC rather than by outstanding amount
- hypothesis: hyp/10
  category: lookup
  input: List all customers from Italy.
  expected: Bruno Serramenti Srl, Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT instead of full name Italy
- hypothesis: hyp/10
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/11
  category: lookup
  input: List all customers from Germany.
  expected: Alpine Foods GmbH
  check: contains
  why: Tests that the agent uses country code 'DE' not 'Germany' when filtering customers
    by country.
- hypothesis: hyp/12
  category: lookup
  input: List all customers based in Germany.
  expected: Alpine Foods GmbH
  check: contains
  why: Tests that the agent uses country code 'DE' not 'Germany' in the SQL query.
- hypothesis: hyp/12
  category: lookup
  input: Which customers are from Spain?
  expected: Casa Verde
  check: contains
  why: Tests that the agent uses country code 'ES' not 'Spain'.
- hypothesis: hyp/12
  category: lookup
  input: List all customers from the Netherlands.
  expected: Delta Logistics
  check: contains
  why: Tests that the agent uses country code 'NL' not 'Netherlands'.
- hypothesis: hyp/13
  category: reasoning
  input: How many days overdue is INV-017 as of today?
  expected: INV-017 was due 2026-05-01; as of 2026-09-01 it is 123 days overdue.
  check: judge
  why: Tests that the agent uses julianday or compute to derive elapsed days rather
    than stating months vaguely.
- hypothesis: hyp/14
  category: reasoning
  input: Did Gallo Ristorazione pay INV-020 on time?
  expected: Yes. INV-020 was due 2026-07-31 and was paid on 2026-07-30, one day before
    the due date.
  check: judge
  why: Tests that the agent retrieves and reports the paid_at date for a specific
    invoice and compares it to due_at.
- hypothesis: hyp/15
  category: reasoning
  input: When did Gallo Ristorazione pay INV-020 relative to its due date?
  expected: INV-020 was due 2026-07-31 and paid on 2026-07-30, one day before the
    due date.
  check: judge
  why: Tests that the agent retrieves and reports the actual payment date from the
    payments table for a paid invoice.
- hypothesis: hyp/16
  category: reasoning
  input: Was INV-020 paid on time?
  expected: Yes. INV-020 was due 2026-07-31 and was paid on 2026-07-30, one day before
    the due date.
  check: judge
  why: Tests that the agent retrieves and reports the actual payment date relative
    to the due date.
- hypothesis: hyp/17
  category: refusal
  input: Will Delta Logistics pay INV-011 before the end of the month?
  expected: refused
  check: refused
  why: Asking for a prediction about future payment behavior, which the ledger cannot
    answer.
- hypothesis: hyp/17
  category: refusal
  input: Do you think Ember Studio will settle their outstanding invoices soon?
  expected: refused
  check: refused
  why: Asking for a forecast about future customer behavior.
- hypothesis: hyp/18
  category: refusal
  input: Will Delta Logistics pay INV-011 this month?
  expected: 'true'
  check: refused
  why: Asking for a future payment prediction, which the ledger cannot answer.
- hypothesis: hyp/18
  category: refusal
  input: Do you think Ember Studio will settle their outstanding balance before year
    end?
  expected: 'true'
  check: refused
  why: Forecasting future customer payment behaviour is outside ledger scope.
