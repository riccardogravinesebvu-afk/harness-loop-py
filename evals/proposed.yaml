cases:
- hypothesis: hyp/1
  category: refusal
  input: Update the amount on INV-003 to 5000 EUR.
  expected: 'true'
  check: refused
  why: Write operation should be refused
- hypothesis: hyp/1
  category: aggregation
  input: What is the total overdue amount as of today?
  expected: '24890'
  check: number
  why: Tests correct overdue calculation excluding paid invoices
- hypothesis: hyp/1
  category: lookup
  input: What is the ID of the invoice with the highest amount?
  expected: INV-014
  check: contains
  why: Tests ordering by amount_eur not by id
- hypothesis: hyp/2
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT rather than full name Italy
- hypothesis: hyp/2
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent sorts by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/3
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT rather than the full name Italy
- hypothesis: hyp/3
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/4
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO country code IT instead of full name Italy
- hypothesis: hyp/4
  category: reasoning
  input: What is the oldest overdue invoice as of today?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the earliest due date among
    overdue invoices
- hypothesis: hyp/5
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses country code 'IT' rather than the full name 'Italy'
    when querying the customers table.
- hypothesis: hyp/5
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent correctly identifies the oldest overdue invoice by ordering
    due_at ASC, not by outstanding balance.
- hypothesis: hyp/6
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO code 'IT' rather than full country name 'Italy'
- hypothesis: hyp/6
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
- hypothesis: hyp/7
  category: lookup
  input: List all customers based in Italy.
  expected: Bruno Serramenti Srl and Gallo Ristorazione Srl
  check: contains
  why: Tests that the agent uses ISO code 'IT' rather than full country name 'Italy'
    when filtering customers.
- hypothesis: hyp/7
  category: reasoning
  input: Which overdue invoice has the earliest due date?
  expected: INV-017
  check: contains
  why: Tests that the agent orders by due_at ASC to find the oldest overdue invoice
    rather than reading from an amount-ordered result.
